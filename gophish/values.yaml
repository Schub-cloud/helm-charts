nameSpace: addons

goPhish:
  resources:
    requests:
      cpu: 1
      memory: 1Gi
    limits:
      cpu: 2
      memory: 2Gi
  svcPorts:
    communication:
      port: 80
      targetPort: 8080
    dashboard:
      port: 3333
      targetPort: 3333
  CIDR: 10.0.0.0/8
  ingress:
    dashboard:
      host: "gophish.schub.cloud"
    #  annotations:
    #    kubernetes.io/ingress.class: nginx
    #    nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
    #  tls: 
    #    - hosts:
    #      secretName: 
    server:
      host: "gophish.server.schub.cloud"
    #  annotations:
    #    kubernetes.io/ingress.class: nginx
    #    nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
    #  tls: 
    #    - hosts:
    #      secretName: 

postfix:
  dkimPrivate: -----BEGIN PRIVATE KEY----- -----END PRIVATE KEY-----
  enabled: true
  email: secops@schub.cloud
  hostname: phishing.schub.cloud
  mail_name: schub.cloud
  port: 25
  resources:
    requests:
      cpu: 0.5
      memory: 500Mi
    limits:
      cpu: 1
      memory: 1Gi
