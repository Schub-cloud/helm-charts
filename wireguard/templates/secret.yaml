apiVersion: v1
kind: Secret
metadata:
  name: {{ include "wireguard.fullname" . }}
type: Opaque
stringData:
  wg0.conf: |
    {{- with .Values.wireguardConfig }}
    wireguardConfig:
      {{- toToml . | nindent 8 }}
    {{- end }}
